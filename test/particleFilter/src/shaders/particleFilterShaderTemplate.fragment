#version 120

uniform float seed;
uniform bool init = false;

void initParticle();
void stepParticle();

void main() {
	if(init)
		initParticle();
	else
		stepParticle();

}



void initParticle(){
	gl_FragColor = seed*vec4(gl_PointCoord.xy, 0.0, 1.0); 

}


void stepParticle(){

}